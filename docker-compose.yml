version: '3.8'

services:
  dspico-builder:
    build: .
    container_name: dspico-builder
    volumes:
      - ${ROMS_DIR:-/tmp/roms}:/roms:ro
      - ${OUTPUT_DIR:-/tmp/output}:/output
    restart: "no" # Set to no so it doesn't loop building
